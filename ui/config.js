export const config = {
    externalBuffers: [
        /*
        {
            id: 200,
            parentId: 1,
            scope: ['custom_key'],
            apply: (view) => ({
                delta: { patch: { custom_key: 'custom_value' } },
                trace: { id: 0, metaBufferId: 200, parentTraceId: null, scope: ['custom_key'] }
            })
        }
        */
    ]
};
