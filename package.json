{
  "name": "metabuffer-system",
  "version": "0.1.0",
  "description": "MetaBuffer System - Core Specifications Implementation",
  "type": "module",
  "scripts": {
    "dev": "neu run",
    "dev:web": "npx serve .",
    "build:neutralino": "neu build",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^1.6.0"
  }
}
